import 'package:flutter/material.dart';
class TVHome extends StatelessWidget {
  const TVHome({super.key});
  @override Widget build(BuildContext context) => Scaffold(body: Center(child: Text("Home TV Ready")));
}
