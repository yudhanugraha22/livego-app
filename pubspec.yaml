name: livego_project
description: "A new Flutter project for LiveGo/CineFlow TV and Mobile."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Koneksi Internet & Enkripsi Keamanan
  dio: 5.4.0
  crypto: 3.0.3

  # Penyimpanan & Database Lokal (HP & TV)
  shared_preferences: 2.2.2
  sqflite: 2.3.0
  path: 1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
